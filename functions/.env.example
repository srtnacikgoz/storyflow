# =================================================================
# Instagram Automation - Environment Variables
# Sade Patisserie
# =================================================================

# =================================================================
# OPENAI API (Required for Vision + DALL-E 3)
# =================================================================
# Get your API key from: https://platform.openai.com/api-keys
# Requires billing setup (credit card)
# Cost: ~$0.01 per Vision analysis, ~$0.08 per DALL-E 3 image
OPENAI_API_KEY=sk-proj-your-api-key-here

# =================================================================
# INSTAGRAM GRAPH API (Required)
# =================================================================
# IMPORTANT: You need an Instagram Business/Creator Account
# connected to a Facebook Page

# --- Step 1: Get Account ID ---
# Run this curl command with your access token:
# curl "https://graph.instagram.com/v18.0/me?fields=id,username&access_token=YOUR_TOKEN"
# The "id" field is your Account ID
INSTAGRAM_ACCOUNT_ID=17841234567890123

# --- Step 2: Get Access Token ---
# Create a Facebook App at: https://developers.facebook.com/apps
# Required permissions:
#   - instagram_basic
#   - instagram_content_publish
#   - pages_read_engagement
#
# For Long-Lived Token (60 days):
# 1. Get short-lived token from Graph API Explorer
# 2. Exchange for long-lived token:
# curl "https://graph.facebook.com/v18.0/oauth/access_token?grant_type=fb_exchange_token&client_id=APP_ID&client_secret=APP_SECRET&fb_exchange_token=SHORT_TOKEN"
INSTAGRAM_ACCESS_TOKEN=EAAxxxxxxxxxxxxxxxxxx

# =================================================================
# FIREBASE FUNCTIONS CONFIG
# =================================================================
# Set these via Firebase CLI (not .env file):
#
# firebase functions:config:set \
#   openai.api_key="sk-proj-..." \
#   instagram.account_id="17841..." \
#   instagram.access_token="EAAxx..."
#
# View current config:
# firebase functions:config:get
#
# =================================================================

# =================================================================
# TOKEN REFRESH REMINDER
# =================================================================
# Instagram access tokens expire in 60 days!
# Set a calendar reminder to refresh before expiration.
#
# Refresh token command:
# curl "https://graph.facebook.com/v18.0/oauth/access_token?grant_type=fb_exchange_token&client_id=APP_ID&client_secret=APP_SECRET&fb_exchange_token=CURRENT_TOKEN"
#
# Then update Firebase config:
# firebase functions:config:set instagram.access_token="NEW_TOKEN"
# firebase deploy --only functions
# =================================================================
