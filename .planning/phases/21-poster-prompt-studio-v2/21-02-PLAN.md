---
phase: 21-poster-prompt-studio-v2
plan: 02
type: execute
domain: firebase-functions-typescript
---

<objective>
generatePosterPrompt endpoint'ine yeni 5 parametre ekle: cameraAngleId, lightingTypeId, backgroundId, negativePrompt, qualityParams. Aspect ratio seed'ine 16:9, 3:4, 4:3 ratioları ekle.

Purpose: Backend'in yeni parametreleri alıp prompt'a dahil etmesi — frontend bunu sonraki planda kullanacak.
Output: generatePosterPrompt 5 yeni parametre kabul eder; prompt çıktısına kamera/ışık/arka plan/negatif/kalite eklenir; aspect ratio listesi genişler.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@functions/src/controllers/orchestrator/posterSmartController.ts
@functions/src/orchestrator/seed/posterData.ts
@functions/src/types/poster.ts
@functions/src/services/config/posterConfig.ts

**Mevcut generatePosterPrompt parametreleri:** productImageBase64, styleId, moodId, aspectRatioId, typographyId, subtitleTypographyId, layoutId, title, subtitle, price, targetModel, includeText, additionalNotes, referenceImageBase64
**Model-specific prompt suffix'leri:** TARGET_MODEL_INSTRUCTIONS objesi mevcut (posterSmartController.ts'de)
**Aspect ratio seed path:** posterData.ts POSTER_ASPECT_RATIOS array'i
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aspect ratio genişletme + seed güncelleme</name>
  <files>functions/src/orchestrator/seed/posterData.ts</files>
  <action>
`POSTER_ASPECT_RATIOS` array'ine şu yeni ratio'ları ekle (mevcut format aynı):

```typescript
{
  id: "16-9",
  label: "16:9 (Yatay)",
  width: 16, height: 9,
  useCase: "YouTube thumbnail, Facebook/LinkedIn kapak",
  promptInstruction: "Landscape 16:9 aspect ratio, wide horizontal composition. Subject centered or using rule of thirds horizontally.",
  isActive: true,
  sortOrder: 5,
},
{
  id: "3-4",
  label: "3:4",
  width: 3, height: 4,
  useCase: "Instagram feed, genel dikey",
  promptInstruction: "Portrait 3:4 ratio, slightly wider than standard portrait. Balanced vertical composition.",
  isActive: true,
  sortOrder: 6,
},
{
  id: "4-3",
  label: "4:3 (Yatay)",
  width: 4, height: 3,
  useCase: "Tablet, genel yatay, Facebook post",
  promptInstruction: "Landscape 4:3 ratio, classic horizontal format. Traditional photography aspect ratio.",
  isActive: true,
  sortOrder: 7,
},
```

seedPosterConfig zaten aspect ratio'ları seed ediyor — yeni ID'ler otomatik eklenecek.
  </action>
  <verify>cd functions && npx tsc --noEmit 2>&1 | head -20</verify>
  <done>TypeScript derleme hatası yok; POSTER_ASPECT_RATIOS'da 7 item var (16-9, 3-4, 4-3 dahil).</done>
</task>

<task type="auto">
  <name>Task 2: generatePosterPrompt'a yeni parametreler + prompt entegrasyonu</name>
  <files>functions/src/controllers/orchestrator/posterSmartController.ts</files>
  <action>
`generatePosterPrompt` fonksiyonunda:

**1. Destructuring'e 5 yeni parametre ekle:**
```typescript
const {
  // ... mevcut parametreler ...
  cameraAngleId,
  lightingTypeId,
  backgroundId,
  negativePrompt,
  qualityMode = "standard", // "standard" | "hd"
} = req.body;
```

**2. Config okuma kısmına 3 yeni getter ekle** (mevcut Promise.all'a ekle):
```typescript
const [style, mood, aspectRatio, typography, subtitleTypography, layout,
       cameraAngle, lightingType, background] = await Promise.all([
  // ... mevcut getterlar ...
  cameraAngleId ? getPosterCameraAngleById(cameraAngleId) : Promise.resolve(null),
  lightingTypeId ? getPosterLightingTypeById(lightingTypeId) : Promise.resolve(null),
  backgroundId ? getPosterBackgroundById(backgroundId) : Promise.resolve(null),
]);
```

**3. Log güncelle** — yeni parametreleri log'a ekle:
```typescript
log("CONFIG", "Firestore config yüklendi", {
  // ... mevcut fields ...
  kameraAçısı: cameraAngle?.name || "otomatik",
  işık: lightingType?.name || "otomatik",
  arkaplan: background?.name || "otomatik",
  negativePrompt: negativePrompt ? "VAR" : "YOK",
  kalite: qualityMode,
});
```

**4. Prompt build bölümünde** (Claude'a giden system prompt içinde), mevcut stil/mood/tipografi/layout talimatlarının altına yeni blok ekle:

```
CAMERA & LIGHTING DIRECTION:
${cameraAngle ? `Camera Angle: ${cameraAngle.promptInstruction}` : ""}
${lightingType ? `Lighting: ${lightingType.promptInstruction}` : ""}
${background ? `Background/Environment: ${background.promptInstruction}` : ""}
```

Bu blok sadece ilgili parametre seçilmişse eklenir (null kontrolü yap).

**5. Model-specific quality suffix** — prompt'un sonuna eklenir (targetModel'e göre):

```typescript
const qualityModelSuffix: Record<string, Record<string, string>> = {
  "midjourney": {
    "standard": "--q 1 --stylize 500",
    "hd": "--q 2 --stylize 750",
  },
  "dall-e": {
    "standard": "", // DALL-E kaliteyi API parametresi ile alıyor, prompt'a ekleme
    "hd": "",
  },
  "gemini": {
    "standard": "",
    "hd": "ultra-detailed, 8K resolution, photorealistic",
  },
  "flux": {
    "standard": "",
    "hd": "ultra detailed, sharp focus, high resolution",
  },
};
const qualitySuffix = qualityModelSuffix[targetModel]?.[qualityMode] || "";
```

**6. Negatif prompt** — sadece Midjourney için prompt sonuna `--no ${negativePrompt}` ekle. Diğer modeller için yanıt objesiyle ayrı `negativePrompt` field'ı döndür (model desteklemiyorsa kullanıcı kopyalayıp ayrı alana yapıştırabilir):

```typescript
// Midjourney: --no suffix
let finalPrompt = generatedPrompt;
if (qualitySuffix) finalPrompt += ` ${qualitySuffix}`;
if (negativePrompt && targetModel === "midjourney") {
  finalPrompt += ` --no ${negativePrompt}`;
}

// Yanıt objesi
res.json({
  success: true,
  data: {
    prompt: finalPrompt,
    negativePrompt: (negativePrompt && targetModel !== "midjourney") ? negativePrompt : undefined,
    // ... mevcut fields ...
  }
});
```

ÖNEMLI: Mevcut prompt üretim akışını bozma. Sadece yeni parametreler için ek satırlar ekle; style/mood/typography/layout sistemi değişmez.
  </action>
  <verify>cd functions && npx tsc --noEmit 2>&1 | head -20</verify>
  <done>TypeScript derleme hatası yok; generatePosterPrompt 5 yeni parametre kabul ediyor; prompt build bölümünde kamera/ışık/arka plan blokları mevcut.</done>
</task>

</tasks>

<verification>
- [ ] `cd functions && npx tsc --noEmit` başarılı (0 hata)
- [ ] POSTER_ASPECT_RATIOS'da 16-9, 3-4, 4-3 ID'li yeni ratio'lar var
- [ ] generatePosterPrompt req.body'den cameraAngleId, lightingTypeId, backgroundId, negativePrompt, qualityMode alıyor
- [ ] Prompt build kısmında kamera/ışık/arka plan talimatları ekleniyor (sadece seçilmişse)
- [ ] Midjourney için --no suffix çalışıyor
- [ ] qualityModelSuffix objesi Midjourney/Gemini/Flux için suffix'leri içeriyor
</verification>

<success_criteria>
- Tüm görevler tamamlandı
- TypeScript derleme hatası yok
- Mevcut poster prompt üretimi bozulmadı
- Yeni parametreler opsiyonel — seçilmezse mevcut davranış korunur
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/21-poster-prompt-studio-v2/21-02-SUMMARY.md` oluştur:

# Phase 21 Plan 02: Prompt Entegrasyonu + Aspect Ratio

**[Substantive one-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
Ready for 21-03-PLAN.md
</output>
