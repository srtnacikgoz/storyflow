---
phase: 21-poster-prompt-studio-v2
plan: 03
type: execute
domain: react-typescript
---

<objective>
Admin panel'e 3 yeni seçici UI ekle (kamera açısı, ışık, arka plan) + negatif prompt alanı. PromptGenerator'ı yeni parametreler + kalite toggle + negatif prompt gösterimiyle güncelle.

Purpose: Backend'i tamamlayan frontend katmanı — kullanıcı artık kamera/ışık/arka plan/negatif/kalite seçebilecek.
Output: Poster.tsx'de 3 yeni seçici + negatif prompt textarea; PromptGenerator.tsx'de kalite toggle + negatif prompt display + tüm yeni props; api.ts'de 3 yeni method.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@admin/src/pages/Poster.tsx
@admin/src/components/poster/PromptGenerator.tsx
@admin/src/services/api.ts

**UI pattern:** Poster.tsx'de mevcut seçiciler grid/flex buton grupları. Yeni seçiciler aynı pattern'i izler.
**API pattern:** api.ts'de `listPosterStyles()` → `GET /listPosterStyles` çağrısı → array döner.
**PromptGenerator pattern:** Props olarak parametre alır, handleGenerate içinde api.generatePosterPrompt'a geçirir.
**CLAUDE.md:** Dosya 400 satır limitine dikkat — Poster.tsx zaten 438 satır, yeni UI bölümleri ayrı component'e taşınabilir ama önce kullanıcıya sor değil, inline ekle (bu küçük eklemeler), gerekirse sonra böl.
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts'e 3 yeni method + Poster.tsx'e yeni state + seçici UI'lar</name>
  <files>admin/src/services/api.ts, admin/src/pages/Poster.tsx</files>
  <action>
**api.ts'e** 3 yeni method ekle (mevcut listPosterLayouts() pattern'ini izle):
```typescript
async listCameraAngles(): Promise<PosterCameraAngle[]>   // GET /listCameraAngles
async listLightingTypes(): Promise<PosterLightingType[]>  // GET /listLightingTypes
async listBackgrounds(): Promise<PosterBackground[]>      // GET /listBackgrounds
```
İlgili interface'leri `admin/src/types/index.ts`'e ekle (poster.ts'deki ile aynı yapı):
```typescript
export interface PosterCameraAngle { id: string; name: string; nameTr: string; description: string; promptInstruction: string; isActive: boolean; sortOrder: number; }
export interface PosterLightingType { ... }
export interface PosterBackground { ... }
```

**Poster.tsx'e** eklemeler:

1. State'ler (mevcut state'lerin altına):
```typescript
const [cameraAngles, setCameraAngles] = useState<PosterCameraAngle[]>([]);
const [lightingTypes, setLightingTypes] = useState<PosterLightingType[]>([]);
const [backgrounds, setBackgrounds] = useState<PosterBackground[]>([]);
const [selectedCameraAngle, setSelectedCameraAngle] = useState("");
const [selectedLightingType, setSelectedLightingType] = useState("");
const [selectedBackground, setSelectedBackground] = useState("");
const [negativePrompt, setNegativePrompt] = useState("");
```

2. loadConfig() Promise.all'a 3 yeni çağrı ekle:
```typescript
const [stylesData, moodsData, ratiosData, typoData, layoutData, cameraData, lightingData, bgData] = await Promise.all([
  // ... mevcut çağrılar ...
  api.listCameraAngles(),
  api.listLightingTypes(),
  api.listBackgrounds(),
]);
setCameraAngles(cameraData.filter(c => c.isActive).sort((a, b) => a.sortOrder - b.sortOrder));
setLightingTypes(lightingData.filter(l => l.isActive).sort((a, b) => a.sortOrder - b.sortOrder));
setBackgrounds(bgData.filter(b => b.isActive).sort((a, b) => a.sortOrder - b.sortOrder));
```

3. JSX'e 3 yeni seçici bölüm ekle (Aspect Ratio bölümünden sonra, Tipografi bölümünden önce):

**Kamera Açısı seçici** (opsiyonel, grid-cols-2, pill-buton stili mevcut moods gibi):
```jsx
{cameraAngles.length > 0 && (
  <div>
    <label className="block text-sm font-medium text-gray-700 mb-2">
      Çekim Açısı <span className="text-xs text-gray-400">(opsiyonel)</span>
    </label>
    <div className="grid grid-cols-2 gap-2">
      <button onClick={() => setSelectedCameraAngle("")} className={`p-2.5 border rounded-xl text-left transition ${!selectedCameraAngle ? "border-gray-900 bg-gray-900 text-white" : "border-gray-200 hover:border-gray-300"}`}>
        <span className="font-medium text-xs">Otomatik</span>
      </button>
      {cameraAngles.map(c => (
        <button key={c.id} onClick={() => setSelectedCameraAngle(c.id)}
          className={`p-2.5 border rounded-xl text-left transition ${selectedCameraAngle === c.id ? "border-emerald-500 bg-emerald-50 ring-1 ring-emerald-500" : "border-gray-200 hover:border-gray-300"}`}
        >
          <span className="font-medium text-xs">{c.nameTr}</span>
          <span className="text-[10px] text-gray-400 block mt-0.5 line-clamp-1">{c.description}</span>
        </button>
      ))}
    </div>
  </div>
)}
```

**Işık seçici** (aynı pattern, renk: orange/amber):
```jsx
{lightingTypes.length > 0 && (
  <div>
    <label ...>Aydınlatma <span ...>(opsiyonel)</span></label>
    <div className="grid grid-cols-2 gap-2">
      <button onClick={() => setSelectedLightingType("")} ...>Otomatik</button>
      {lightingTypes.map(l => (
        <button key={l.id} onClick={() => setSelectedLightingType(l.id)}
          className={`... ${selectedLightingType === l.id ? "border-orange-500 bg-orange-50 ring-1 ring-orange-500" : "..."}`}
        >
          <span ...>{l.nameTr}</span>
          <span ...>{l.description}</span>
        </button>
      ))}
    </div>
  </div>
)}
```

**Arka Plan seçici** (aynı pattern, renk: teal):
```jsx
{backgrounds.length > 0 && (
  <div>
    <label ...>Arka Plan <span ...>(opsiyonel)</span></label>
    <div className="grid grid-cols-2 gap-2">
      <button onClick={() => setSelectedBackground("")} ...>Otomatik</button>
      {backgrounds.map(b => (
        <button key={b.id} onClick={() => setSelectedBackground(b.id)}
          className={`... ${selectedBackground === b.id ? "border-teal-500 bg-teal-50 ring-1 ring-teal-500" : "..."}`}
        >
          <span ...>{b.nameTr}</span>
          <span ...>{b.description}</span>
        </button>
      ))}
    </div>
  </div>
)}
```

**Negatif Prompt alanı** (Ek Notlar alanından sonra, PromptGenerator'dan önce):
```jsx
<div>
  <label className="block text-xs font-medium text-gray-600 mb-1">
    Negatif Prompt <span className="text-gray-400">(istemediğin öğeler)</span>
  </label>
  <input
    type="text"
    value={negativePrompt}
    onChange={e => setNegativePrompt(e.target.value)}
    placeholder="blurry, text overlay, watermark, distorted..."
    className="w-full border rounded-lg px-3 py-2 text-sm"
  />
  <p className="text-[10px] text-gray-400 mt-1">Midjourney'de --no prefix otomatik eklenir</p>
</div>
```

4. PromptGenerator'a yeni props geçir:
```jsx
<PromptGenerator
  // ... mevcut props ...
  cameraAngleId={selectedCameraAngle || undefined}
  lightingTypeId={selectedLightingType || undefined}
  backgroundId={selectedBackground || undefined}
  negativePrompt={negativePrompt || undefined}
/>
```
  </action>
  <verify>cd admin && npm run build 2>&1 | tail -20</verify>
  <done>Build başarılı, TypeScript hatası yok; Poster.tsx'de 4 yeni bölüm (kamera, ışık, arka plan, negatif prompt) mevcut.</done>
</task>

<task type="auto">
  <name>Task 2: PromptGenerator.tsx güncelleme — yeni props + kalite toggle + negatif prompt display</name>
  <files>admin/src/components/poster/PromptGenerator.tsx</files>
  <action>
**Props interface'ine** 5 yeni opsiyonel field ekle:
```typescript
interface PromptGeneratorProps {
  // ... mevcut props ...
  cameraAngleId?: string;
  lightingTypeId?: string;
  backgroundId?: string;
  negativePrompt?: string;
  qualityMode?: "standard" | "hd"; // default: "standard"
}
```

**State'e** kalite modu ekle:
```typescript
const [qualityMode, setQualityMode] = useState<"standard" | "hd">("standard");
```

**handleGenerate'de** yeni parametreler API çağrısına ekle:
```typescript
const res = await api.generatePosterPrompt({
  // ... mevcut parametreler ...
  cameraAngleId: props.cameraAngleId,
  lightingTypeId: props.lightingTypeId,
  backgroundId: props.backgroundId,
  negativePrompt: props.negativePrompt,
  qualityMode,
});
```

**UI'ya kalite toggle ekle** (includeText toggle'ından sonra):
```jsx
<div className="flex items-center justify-between bg-gray-50 rounded-lg p-3">
  <div>
    <p className="text-sm font-medium text-gray-700">HD Kalite</p>
    <p className="text-xs text-gray-400">Midjourney: --q 2, Gemini/Flux: ultra-detailed suffix</p>
  </div>
  <button
    onClick={() => { setQualityMode(v => v === "hd" ? "standard" : "hd"); setResult(null); }}
    className={`w-12 h-6 rounded-full transition ${qualityMode === "hd" ? "bg-amber-500" : "bg-gray-300"}`}
  >
    <div className={`w-5 h-5 bg-white rounded-full shadow transition transform ${qualityMode === "hd" ? "translate-x-6" : "translate-x-0.5"}`} />
  </button>
</div>
```

**Seçili parametre özeti** (referans poster indicator'ın altına, generate butonundan önce):
```jsx
{(props.cameraAngleId || props.lightingTypeId || props.backgroundId || props.negativePrompt) && (
  <div className="bg-gray-50 rounded-lg p-3 text-xs text-gray-500 space-y-1">
    <p className="font-medium text-gray-600 text-xs mb-1">Aktif Parametreler</p>
    {props.cameraAngleId && <p>📷 Çekim açısı seçildi</p>}
    {props.lightingTypeId && <p>💡 Aydınlatma seçildi</p>}
    {props.backgroundId && <p>🖼 Arka plan seçildi</p>}
    {props.negativePrompt && <p>🚫 Negatif: {props.negativePrompt.substring(0, 40)}{props.negativePrompt.length > 40 ? "..." : ""}</p>}
  </div>
)}
```

**Sonuç bölümüne negativePrompt gösterimi ekle** (DALL-E/Gemini/Flux için ayrı alan olarak — Midjourney'de prompt'a dahil edildiği için görünmez):
```jsx
{result.negativePrompt && (
  <div className="bg-red-50 border border-red-100 rounded-lg p-3">
    <p className="text-xs font-medium text-red-600 mb-1">Negatif Prompt (ayrı alana yapıştır)</p>
    <div className="relative">
      <pre className="text-sm text-red-800 font-mono">{result.negativePrompt}</pre>
      <button onClick={() => navigator.clipboard.writeText(result.negativePrompt!)}
        className="absolute top-1 right-1 text-xs bg-red-100 hover:bg-red-200 text-red-600 px-2 py-0.5 rounded">
        Kopyala
      </button>
    </div>
  </div>
)}
```

Result type güncellemesi — `result` state type'ına `negativePrompt?: string` ekle.
  </action>
  <verify>cd admin && npm run build 2>&1 | tail -20</verify>
  <done>Build başarılı; PromptGenerator'da kalite toggle, parametre özeti ve negatif prompt display mevcut.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 21 tüm görsel parametre UI'ları:
    - Poster.tsx'de Çekim Açısı, Aydınlatma, Arka Plan seçicileri (Firestore'dan yükleniyor)
    - Negatif prompt input alanı
    - PromptGenerator'da HD kalite toggle
    - Aktif parametre özeti
    - Negatif prompt sonuç gösterimi (DALL-E/Gemini/Flux için)
  </what-built>
  <how-to-verify>
    1. `cd admin && npm run dev` ile admin paneli başlat
    2. Poster sayfasına git
    3. Aspect Ratio bölümünde 16:9, 3:4, 4:3 seçeneklerinin görünüp görünmediğini kontrol et
       (Görünmüyorsa: Seed çalıştır — Poster Config Seed Et butonu veya API çağrısı)
    4. Çekim Açısı bölümünün yüklendiğini ve 5 seçenek gösterdiğini doğrula
       (Boşsa: Backend deploy edilmedi demek — bu normal, UI'ın render ettiğini doğrula)
    5. Aydınlatma bölümünde 6 seçenek görünüyor mu?
    6. Arka Plan bölümünde 7 seçenek görünüyor mu?
    7. Negatif prompt input alanı Ek Notlar'ın altında görünüyor mu?
    8. "Prompt Üret" butonuna tıkla → PromptGenerator açılıyor mu?
    9. PromptGenerator'da "HD Kalite" toggle görünüyor mu?
    10. Herhangi bir seçim yapıldıysa "Aktif Parametreler" özeti görünüyor mu?
    11. Build hatası yok mu: `cd admin && npm run build`
  </how-to-verify>
  <resume-signal>
    "approved" yaz (UI görsel açıdan doğru ve build başarılı)
    veya gördüğün sorunları açıkla
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] `cd admin && npm run build` başarılı (0 hata)
- [ ] Poster.tsx'de kamera, ışık, arka plan, negatif prompt bölümleri mevcut
- [ ] PromptGenerator.tsx'de HD toggle, parametre özeti, negativePrompt display mevcut
- [ ] api.ts'de listCameraAngles, listLightingTypes, listBackgrounds metodları mevcut
- [ ] types/index.ts'de 3 yeni interface mevcut
</verification>

<success_criteria>
- Tüm görevler tamamlandı
- Admin build başarılı
- Poster.tsx 400 satır limitini aşmamışsa sorun yok; aştıysa notla (sonraki fazda böl)
- Mevcut poster prompt üretimi bozulmadı
- Phase 21 tüm planları tamamlandı
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/21-poster-prompt-studio-v2/21-03-SUMMARY.md` oluştur:

# Phase 21 Plan 03: Frontend UI Güncelleme

**[Substantive one-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
Phase 21 tamamlandı, Phase 22'ye hazır (UX & Akış İyileştirmeleri)
</output>
