---
phase: 24-model-config-centralization
plan: 02
type: execute
domain: react-admin
---

<objective>
Admin Panel'deki model seçim arayüzünü tek tutarlı yapıya kavuştur.

Purpose: Şu an iki farklı section (APISettingsSection + AIModelSection) birbirleriyle çakışan model seçimleri sunuyor. Aynı `imageModel` alanı iki yerden kaydedilebiliyor. Bu plan sorumluluğu netleştirir: APISettingsSection sadece API key yönetimi yapar, AIModelSection tüm model seçimlerini kapsar.

Output:
- `APISettingsSection.tsx` — model seçim state'leri ve kayıt kodu kaldırıldı, sadece API key yönetimi
- `AIModelSection.tsx` — 24-01'de eklenen 4 yeni Firestore alanını da kapsıyor, tüm model görevleri burada
- Sayfa: "AI Model Seçimi" altında gruplandırılmış: İçerik Üretim, Önizleme, Enhancement Analiz, Visual Critic, Poster Analiz, Poster Learning, Poster Görsel, Poster Prompt
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@admin/src/pages/settings/AIModelSection.tsx
@admin/src/pages/settings/APISettingsSection.tsx
@admin/src/pages/Settings.tsx
@admin/src/services/api.ts
@functions/src/orchestrator/types.ts

**Mevcut AIModelSection durumu:**
- `imageModel` (İçerik Üretim) — kaydediyor ✓
- `posterAnalysisModel` (Vision Analysis) — kaydediyor ✓
- EKSIK: `visualCriticModel`, `posterLearningModel`, `previewImageModel`, `analysisModel`, `posterImageModel`, `posterPromptModel`

**Mevcut APISettingsSection model seçimleri (kaldırılacak):**
- `posterPromptModel` state + kaydı → AIModelSection'a taşı
- `analysisModel` state + kaydı → AIModelSection'a taşı
- `posterImageModel` state + kaydı → AIModelSection'a taşı
- `contentImageModel` state + kaydı → AIModelSection'da zaten var (`imageModel`)
- Bu 4 field'ın UI'daki model seçim alanları, modelsLoading, textModels, imageModels dinamik listesi → kaldırılacak
- Kalacak: API key giriş alanları (openRouter, anthropic, openai, google), `preferredProvider`, key kayıt fonksiyonu

**Eklenmesi gereken görevler (AIModelSection'a):**
1. `previewImageModel` — Senaryo Önizleme (flash model listesinden seç)
2. `visualCriticModel` — Visual Critic Analiz (Gemini modelleri)
3. `posterLearningModel` — Poster Learning (Claude/metin modelleri)
4. `analysisModel` — Enhancement Analiz (Gemini modelleri)
5. `posterImageModel` — Poster Görsel Üretim (görsel model listesi)
6. `posterPromptModel` — Poster Prompt Yazıcı (metin model listesi)

**Mevcut IMAGE_MODELS listesi (AIModelSection'da):**
```ts
const IMAGE_MODELS = [
  { id: "gemini-3-pro-image-preview", label: "Gemini 3 Pro Image", note: "...", cost: "$0.04/görsel" },
  { id: "gemini-3.1-flash-image-preview", label: "Gemini 3.1 Flash Image", note: "...", cost: "~$0.00" },
];
```

**CLAUDE.md Kuralları:**
- Tek karar noktası: Aynı field iki yerden yönetilmez
- Önce kaldır sonra ekle
- Dosya boyutu limiti (400 satır): AIModelSection şu an ~288 satır, yeni içerikle dolacak — gerekirse alt component'lere böl
</context>

<tasks>

<task type="auto">
  <name>Task 1: APISettingsSection — model seçimlerini temizle</name>
  <files>admin/src/pages/settings/APISettingsSection.tsx</files>
  <action>
`APISettingsSection.tsx`'tan aşağıdakileri tamamen kaldır:

**Kaldırılacak state'ler:**
- `posterPromptModel` ve setter
- `analysisModel` ve setter (satır 33)
- `posterImageModel` ve setter (satır 34)
- `contentImageModel` ve setter (satır 35)
- `textModels` state ve setter
- `imageModels` state ve setter
- `modelsLoading` state ve setter

**Kaldırılacak fonksiyonlar:**
- `loadModels` callback
- `handleSeedModels`

**Kaldırılacak load mantığı (`loadSettings` içinden):**
- `posterPromptModel` yükleme satırı
- `analysisModel` yükleme satırı
- `posterImageModel` yükleme satırı
- `contentImageModel` yükleme satırı (imageModel)
- `preferredProvider` yükleme — kalıyor (provider tercihi API key ile ilgili)

**Kaldırılacak save mantığı (`handleSaveModels` veya benzeri):**
- Model seçimlerini kaydeden tüm `api.updateSystemSettings({ posterPromptModel, ... })` çağrısı

**Kaldırılacak UI bölümleri:**
- "Model Seçimi" başlıklı tüm section (textModels/imageModels listesi, seed butonu)
- Model seçim radio/button grupları

**Kalacaklar:**
- PROVIDERS array ve key giriş UI
- `keys`, `hasKeys`, `keySaving` state'leri
- `loadSettings` sadece key durumunu yükler
- `handleSaveKeys` — key kayıt
- `preferredProvider` (hangi API'nin kullanılacağı — key yönetimiyle ilgili)
- API bilgi/seed butonu (listTextModels/listImageModels için değil, key kontrolü için)

NOT: `modelsLoading`, `api.listTextModels()`, `api.listImageModels()`, `api.seedModelRegistry()` çağrıları bu dosyadan tamamen kalkıyor.
  </action>
  <verify>cd admin && npm run build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -20</verify>
  <done>Build hatasız. APISettingsSection sadece API key yönetimi yapıyor. "posterPromptModel", "contentImageModel", "analysisModel", "posterImageModel" state'leri yok.</done>
</task>

<task type="auto">
  <name>Task 2: AIModelSection — tüm model görevleri</name>
  <files>admin/src/pages/settings/AIModelSection.tsx</files>
  <action>
`AIModelSection.tsx`'ı tüm model görevlerini kapsayacak şekilde genişlet.

**Mevcut yapı (koru):**
- IMAGE_MODELS listesi (görsel üretim için)
- VISION_MODELS listesi (analiz için)
- TIER_LABELS
- `imageModel` state → "İçerik Görsel Üretim Modeli" bölümü
- `analysisModels` state (Record<string,string>) → "Vision Analysis" bölümü (posterAnalysisModel)

**Eklenecek alanlar — ANALYSIS_TASKS array'ine ekle:**
```ts
const ANALYSIS_TASKS = [
  {
    key: "posterAnalysisModel",
    label: "Poster Analiz Modeli",
    description: "Referans poster yüklendiğinde stil, renk paleti, tipografi ve kompozisyon analizi",
    modelList: "vision",  // VISION_MODELS kullan
  },
  {
    key: "analysisModel",
    label: "Enhancement Analiz Modeli",
    description: "Fotoğraf iyileştirme — arka plan, stil ve ürün analizi (Gemini modeli gerekir)",
    modelList: "gemini-text",  // Gemini text modelleri — sadece Gemini olanlar
  },
  {
    key: "visualCriticModel",
    label: "Visual Critic Modeli",
    description: "Üretilen görselin kalite değerlendirmesi — puan ve iyileştirme önerileri",
    modelList: "vision",  // Hem görsel anlama hem text üretim yapabilmeli
  },
  {
    key: "posterLearningModel",
    label: "Poster Learning Modeli",
    description: "Kullanıcı feedback'lerinden stil düzeltmeleri çıkarır (metin analiz)",
    modelList: "vision",  // Claude/metin modelleri
  },
];
```

**Eklenecek IMAGE bölümü — mevcut "İçerik Görsel Üretim" altına:**
Yeni radio grup olarak `previewImageModel` ekle:
```
Senaryo Önizleme Modeli — Senaryo seçim ekranında 4 paralel varyasyon üretir (hız önemli, flash önerilir)
```
IMAGE_MODELS listesinden seçim.

**Eklenecek IMAGE bölümü — Poster için:**
Mevcut IMAGE_MODELS'e Poster'a özel modeller de ekleniyor. Ya IMAGE_MODELS aynı listeyi kullanır, ya da posterImageModel için ayrı bir liste kullan:
```ts
const POSTER_IMAGE_MODELS = [
  { id: "google/gemini-2.5-flash-image", label: "Gemini 2.5 Flash Image", note: "Poster için öneri", cost: "~$0.01/görsel" },
  { id: "gemini-3-pro-image-preview", label: "Gemini 3 Pro Image", note: "Yüksek kalite", cost: "$0.04/görsel" },
  { id: "gemini-3.1-flash-image-preview", label: "Gemini 3.1 Flash Image", note: "Dengeli", cost: "~$0.00" },
];
```
`posterImageModel` state ekle, bu listeden seçim.

**posterPromptModel için:**
VISION_MODELS listesini kullan (metin modeli). `posterPromptModel` state ekle.

**State güncelleme:**
```ts
// Mevcut
const [imageModel, setImageModel] = useState("gemini-3-pro-image-preview");
const [analysisModels, setAnalysisModels] = useState<Record<string, string>>({...});

// Ekle
const [previewImageModel, setPreviewImageModel] = useState("gemini-3.1-flash-image-preview");
const [posterImageModel, setPosterImageModel] = useState("google/gemini-2.5-flash-image");
const [posterPromptModel, setPosterPromptModel] = useState("anthropic/claude-haiku-4.5");
```

**load fonksiyonu** — yeni alanları da yükle:
```ts
if (settings.previewImageModel) setPreviewImageModel(settings.previewImageModel);
if (settings.posterImageModel) setPosterImageModel(settings.posterImageModel);
if ((settings as any).posterPromptModel) setPosterPromptModel((settings as any).posterPromptModel);
// analysisModels'i genişlet — yeni task key'lerini de yükle
```

**handleSave** — yeni alanları da kaydet:
```ts
await api.updateSystemSettings({
  imageModel,
  previewImageModel,
  posterImageModel,
  posterPromptModel,
  ...analysisModels,  // posterAnalysisModel, analysisModel, visualCriticModel, posterLearningModel
});
```

**UI organizasyonu — gruplandırma:**

```
──── GÖRSEL ÜRETİM ────────────────────────
  📷 İçerik Görsel Üretim Modeli     [imageModel — IMAGE_MODELS]
  👁️ Senaryo Önizleme Modeli         [previewImageModel — IMAGE_MODELS]
  🎨 Poster Görsel Üretim Modeli     [posterImageModel — POSTER_IMAGE_MODELS]

──── ANALİZ & METİN ───────────────────────
  ✍️ Poster Prompt Yazıcı            [posterPromptModel — VISION_MODELS]
  🔬 Poster Analiz Modeli            [posterAnalysisModel — VISION_MODELS]
  🔍 Enhancement Analiz Modeli       [analysisModel — VISION_MODELS]
  👨‍⚖️ Visual Critic Modeli           [visualCriticModel — VISION_MODELS]
  🧠 Poster Learning Modeli          [posterLearningModel — VISION_MODELS]
```

Her grup için bir `<div className="card">` kullan — mevcut card stilini koru.

**Dosya boyutu kontrolü:** Eğer 400 satırı geçecekse, görsel üretim kartını `ImageModelCard.tsx`, analiz kartlarını `AnalysisModelCard.tsx` alt component'ine taşı, AIModelSection bunları import etsin. Her sub-component kendi props'unu alır.
  </action>
  <verify>cd admin && npm run build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -20</verify>
  <done>Build hatasız. AIModelSection: 8 farklı model görevi var. Tüm yeni alanlar yükleniyor ve kaydediliyor. APISettingsSection'daki model seçimleriyle çakışma yok.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - APISettingsSection: Sadece API key yönetimi — model seçim UI'ı yok
    - AIModelSection: Görsel üretim + analiz gruplarında 8 model görevi
    - İki section arasında çakışma yok
  </what-built>
  <how-to-verify>
    1. `cd admin && npm run dev` çalıştır
    2. Ayarlar > API Ayarları sayfasını aç
       - Model seçim dropdownları/radioları OLMAMALI
       - Sadece provider API key alanları görünmeli
    3. Ayarlar > AI Model Seçimi sayfasını aç
       - "Görsel Üretim" grubunda: İçerik, Önizleme, Poster Görsel — 3 alan
       - "Analiz & Metin" grubunda: Prompt Yazıcı, Poster Analiz, Enhancement Analiz, Visual Critic, Learning — 5 alan
       - Tüm alanlar yüklendiğinde mevcut Firestore değerlerini göstermeli
    4. Bir model değiştir ve "Kaydet" tıkla — başarılı mesajı çıkmalı
    5. Sayfayı yenile — değişiklik korunmuş olmalı
  </how-to-verify>
  <resume-signal>Onaylıyorum — ya da tespit ettiğin sorunu yaz</resume-signal>
</task>

</tasks>

<verification>
- [ ] `cd admin && npm run build` hatasız
- [ ] `grep -n "posterPromptModel\|analysisModel\|posterImageModel\|contentImageModel" admin/src/pages/settings/APISettingsSection.tsx` → sadece import varsa bile 0 satır çıkmalı (model state yok)
- [ ] `grep -c "previewImageModel\|visualCriticModel\|posterLearningModel" admin/src/pages/settings/AIModelSection.tsx` → 3+ sonuç
</verification>

<success_criteria>
- APISettingsSection: Model seçim kodu yok, sadece API key yönetimi
- AIModelSection: 8 model görevi var, 2 gruba ayrılmış
- Her model görevi Firestore'a kaydediliyor ve geri yükleniyor
- TypeScript build hatasız (hem functions hem admin)
- Çakışan alan yönetimi yok
</success_criteria>

<output>
Tamamlanınca `.planning/phases/24-model-config-centralization/24-02-SUMMARY.md` oluştur:

# Phase 24 Plan 02: Admin Panel Model Config Summary

**[Tek cümle]**

## Accomplishments
- [...]

## Files Created/Modified
- `admin/src/pages/settings/APISettingsSection.tsx` — model seçimler kaldırıldı
- `admin/src/pages/settings/AIModelSection.tsx` — 8 model görevi eklendi
- `admin/src/pages/settings/[sub-components].tsx` — (varsa) bölünmüş component'ler

## Decisions Made
[...]

## Issues Encountered
[...]

## Next Step
Phase 24 tamamlandı. Deploy: `cd functions && npm run build && firebase deploy --only functions`
</output>
