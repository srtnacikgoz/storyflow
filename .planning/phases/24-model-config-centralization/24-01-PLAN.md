---
phase: 24-model-config-centralization
plan: 01
type: execute
domain: firebase-functions
---

<objective>
Backend'deki tüm hardcoded model string'lerini Firestore-driven hale getir.

Purpose: Sistemin herhangi bir yerinde `"gemini-3-pro-image-preview"`, `"claude-haiku-4-5-20251001"` gibi model string'leri sabit kodlanmış kalıyor. Bunlar deploy gerektirmeden Admin Panel'den değiştirilemez. Bu plan backend'de tam çözüm sağlar.

Output:
- `SystemSettings` tipine 4 yeni alan eklendi
- `defaultData.ts`'de bu alanlar için default değerler tanımlı
- `shared.ts` ve `orchestratorScheduler.ts` imageModel'i Firestore'dan okuyor
- `visualCriticService`, `posterLearningService`, `posterSmartController`, `enhancementController` içindeki hardcoded model string'leri kaldırıldı
- `queue.ts`'teki eski/geçersiz `"gemini-flash"` fallback temizlendi
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@functions/src/orchestrator/types.ts
@functions/src/orchestrator/seed/defaultData.ts
@functions/src/controllers/orchestrator/shared.ts
@functions/src/schedulers/orchestratorScheduler.ts
@functions/src/services/visualCriticService.ts
@functions/src/services/posterLearningService.ts
@functions/src/controllers/orchestrator/posterSmartController.ts
@functions/src/controllers/orchestrator/enhancementController.ts
@functions/src/services/queue.ts
@functions/src/services/configService.ts

**Mevcut SystemSettings alanları (types.ts ~1919 satır):**
- `imageModel: string` — İçerik üretim model (scheduler/orchestrator)
- `posterPromptModel?: string` — Poster prompt yazıcı
- `posterImageModel?: string` — Poster görsel üretim
- `analysisModel?: string` — Enhancement analiz modeli (type'da var ama `as any` ile erişiliyor)
- `posterAnalysisModel` — Poster analiz modeli (type'da YOK, `as any` ile erişiliyor)

**Eklenmesi gereken yeni alanlar:**
- `posterAnalysisModel?: string` — Poster analiz (posterSmartController kullanıyor, type'da eksik)
- `visualCriticModel?: string` — VisualCriticService (şu an tamamen hardcoded)
- `posterLearningModel?: string` — PosterLearningService (şu an tamamen hardcoded)
- `previewImageModel?: string` — Senaryo önizleme için flash model (scenarioController, orchestrator:1446)

**Mevcut hardcoded konumlar (kritik):**
- `visualCriticService.ts:42` → `private modelName = "gemini-3-pro-image-preview"` — hiç config okumaz
- `posterLearningService.ts:78` → `model: "claude-haiku-4-5-20251001"` — hiç config okumaz
- `posterSmartController.ts:155` → okunuyor ama satır 553'te değişken yerine `"claude-haiku-4-5-20251001"` kullanılıyor
- `enhancementController.ts:160` → `|| "gemini-2.0-flash"` sessiz fallback
- `enhancementController.ts:284` → `|| "gemini-3.1-flash-image-preview"` sessiz fallback
- `shared.ts:80` → `geminiModel: "gemini-3-pro-image-preview"` — Firestore okumuyor
- `orchestratorScheduler.ts:36` → `geminiModel: "gemini-3-pro-image-preview"` — getOrchestratorConfig() kullanmıyor
- `queue.ts:54,97,658` → `|| "gemini-flash"` — eski/geçersiz model adı

**CLAUDE.md Kuralları:**
- Sessiz fallback YASAK (MEMORY.md): Firestore'da değer yoksa fallback değil, net hata ver
- Dinamik mimari: hardcoded string YASAK, Firestore'dan oku
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemSettings tipi + defaultData + shared.ts + scheduler</name>
  <files>
    functions/src/orchestrator/types.ts,
    functions/src/orchestrator/seed/defaultData.ts,
    functions/src/controllers/orchestrator/shared.ts,
    functions/src/schedulers/orchestratorScheduler.ts
  </files>
  <action>
**1a. `types.ts` — SystemSettings interfacine 4 yeni alan ekle (~1944. satır `posterModel?` bloğunun yanına):**
```ts
posterAnalysisModel?: string;    // Poster analiz modeli (default: "claude-haiku-4-5-20251001")
visualCriticModel?: string;      // Visual critic analiz modeli (default: "gemini-3-pro-image-preview")
posterLearningModel?: string;    // Poster learning/feedback analiz modeli (default: "claude-haiku-4-5-20251001")
previewImageModel?: string;      // Senaryo/önizleme görsel üretim modeli (default: "gemini-3.1-flash-image-preview")
```
Ayrıca `analysisModel?: string` zaten var ama "as any" ile kullanılıyor — tip güvenli olduğunu kontrol et (zaten tipte varsa dokunma).

**1b. `defaultData.ts` — DEFAULT_SYSTEM_SETTINGS objesine yeni alanları ekle (~500. satır imageModel'in yanına):**
```ts
posterAnalysisModel: "claude-haiku-4-5-20251001",
visualCriticModel: "gemini-3-pro-image-preview",
posterLearningModel: "claude-haiku-4-5-20251001",
previewImageModel: "gemini-3.1-flash-image-preview",
analysisModel: "gemini-2.0-flash",
```

**1c. `shared.ts` — `getOrchestratorConfig()` fonksiyonunda (satır 64-95) `geminiModel` alanını Firestore'dan oku:**
Mevcut kod `geminiModel: "gemini-3-pro-image-preview"` hardcoded. Düzelt:
```ts
geminiModel: systemSettings?.imageModel || throw hata
```
Ama dikkat: `geminiModel` alanı `OrchestratorConfig`'de var mı kontrol et (`functions/src/orchestrator/types.ts`'teki `OrchestratorConfig` interfacine bak). Varsa `systemSettings.imageModel` değerini kullan. systemSettings.imageModel boşsa hata fırlat: "Görsel üretim modeli Firestore'da tanımlı değil (Ayarlar > AI Model Seçimi)".

**1d. `orchestratorScheduler.ts` — `orchestratorConfig` objesi içindeki (satır 34-44) `geminiModel` hardcoded'ı kaldır:**
Bu scheduler `getOrchestratorConfig()` kullanmıyor, kendi inline config yazıyor. Düzelt:
```ts
const orchestratorConfig = await getOrchestratorConfig();
// Ardından SchedulerClass'ı bu config ile başlat
```
NOT: `getOrchestratorConfig()` `shared.ts`'te tanımlı, import ekle. Mevcut secret tanımları (geminiApiKey vb.) scheduler scope'unda kalmalı — `getOrchestratorConfig()` config'i oluştururken bu secret değerleri nasıl alıyor kontrol et, gerekirse scheduler'dan config'e geçir.
  </action>
  <verify>cd functions && npm run build 2>&1 | grep -E "error TS|Error" | head -20</verify>
  <done>TypeScript build hatasız geçer. defaultData'da 5 yeni alan var. shared.ts imageModel'i Firestore'dan okuyor. Scheduler getOrchestratorConfig() kullanıyor.</done>
</task>

<task type="auto">
  <name>Task 2: Servis ve controller hardcoded model fix</name>
  <files>
    functions/src/services/visualCriticService.ts,
    functions/src/services/posterLearningService.ts,
    functions/src/controllers/orchestrator/posterSmartController.ts,
    functions/src/controllers/orchestrator/enhancementController.ts,
    functions/src/services/queue.ts
  </files>
  <action>
**2a. `visualCriticService.ts` — constructor'a systemSettings okuma ekle:**
Mevcut: `private modelName = "gemini-3-pro-image-preview"` sınıf field'ı.
VisualCriticService'in `critique()` metodunu bul. Metod başında:
```ts
const { getSystemSettings } = await import("../services/configService");
const settings = await getSystemSettings();
if (!settings?.visualCriticModel) {
  throw new Error("Visual critic modeli Firestore'da tanımlı değil (Ayarlar > AI Model Seçimi)");
}
const modelToUse = settings.visualCriticModel;
```
`this.modelName` yerine `modelToUse` kullan. `private modelName` field'ını kaldır.
NOT: getSystemSettings import path'ini servise göre ayarla (../services/configService veya ./configService).

**2b. `posterLearningService.ts` — satır 78'deki hardcoded model:**
`anthropic.messages.create({ model: "claude-haiku-4-5-20251001", ... })` çağrısından önce:
```ts
const { getSystemSettings } = await import("../services/configService");
const settings = await getSystemSettings();
if (!settings?.posterLearningModel) {
  throw new Error("Poster learning modeli Firestore'da tanımlı değil (Ayarlar > AI Model Seçimi)");
}
```
`model: "claude-haiku-4-5-20251001"` → `model: settings.posterLearningModel`

**2c. `posterSmartController.ts` — satır 553'teki hardcoded:**
Satır 155: `const analysisModel = (systemSettings as any)?.posterAnalysisModel || "claude-haiku-4-5-20251001"` → fallback kaldır, hata ver:
```ts
const analysisModel = systemSettings?.posterAnalysisModel;
if (!analysisModel) {
  res.status(400).json({ success: false, error: "Poster analiz modeli tanımlı değil (Ayarlar > AI Model Seçimi)" });
  return;
}
```
Satır 528 ve 553: `model: "claude-haiku-4-5-20251001"` → `model: analysisModel`
(İki yerde var — her ikisini de düzelt)

**2d. `enhancementController.ts` — iki fallback:**
Satır 160: `|| "gemini-2.0-flash"` → kaldır, hata ver:
```ts
const analysisModel = systemSettings?.analysisModel;
if (!analysisModel) {
  res.status(400).json({ success: false, error: "Enhancement analiz modeli tanımlı değil (Ayarlar > AI Model Seçimi)" });
  return;
}
```
Satır 284: `|| "gemini-3.1-flash-image-preview"` → kaldır, hata ver:
```ts
const imageModel = systemSettings?.imageModel;
if (!imageModel) {
  res.status(400).json({ success: false, error: "İçerik görsel üretim modeli tanımlı değil (Ayarlar > AI Model Seçimi)" });
  return;
}
```

**2e. `queue.ts` — eski `"gemini-flash"` fallback (satır 54, 97, 658):**
Bu fallback değerleri bir default'a ihtiyaç duyuyor — queue servis seviyesi bir şey, Firestore okumak pahalı olabilir. Yerine geçerli `"gemini-3-pro-image-preview"` kullan:
`|| "gemini-flash"` → `|| "gemini-3-pro-image-preview"`
NOT: Bu queue içindeki aiModel field'ı — pipeline zaten Firestore'dan gerçek model seçimini okuduğundan bu fallback kritik değil. `"gemini-flash"` artık var olmayan bir model adı olduğu için geçerli bir string'e güncelle.
  </action>
  <verify>cd functions && npm run build 2>&1 | grep -E "error TS|Error" | head -20</verify>
  <done>Build hatasız. visualCriticService ve posterLearningService hardcoded model string içermiyor. posterSmartController'da analysisModel değişkeni kullanılıyor. enhancementController fallback yok, hata fırlatıyor. queue.ts'te "gemini-flash" string'i yok.</done>
</task>

</tasks>

<verification>
Build sonrası kontroller:
- [ ] `cd functions && npm run build` hatasız geçmeli
- [ ] `grep -r '"gemini-flash"' functions/src/` — sonuç yok (eski model adı)
- [ ] `grep -n '"gemini-3-pro-image-preview"' functions/src/services/visualCriticService.ts` — sonuç yok
- [ ] `grep -n '"claude-haiku-4-5-20251001"' functions/src/services/posterLearningService.ts` — sonuç yok
- [ ] `grep -n '"claude-haiku-4-5-20251001"' functions/src/controllers/orchestrator/posterSmartController.ts` — sonuç yok
- [ ] `grep -c 'geminiModel: "gemini' functions/src/controllers/orchestrator/shared.ts` — 0 çıkmalı
- [ ] `grep -c 'geminiModel: "gemini' functions/src/schedulers/orchestratorScheduler.ts` — 0 çıkmalı
</verification>

<success_criteria>
- SystemSettings tipinde 4 yeni alan (posterAnalysisModel, visualCriticModel, posterLearningModel, previewImageModel)
- defaultData.ts'de bu 4 alanın default değerleri
- shared.ts ve orchestratorScheduler.ts imageModel'i Firestore'dan okuyor
- 4 servis/controller: hiçbirinde hardcoded model string yok, Firestore boşsa açık hata mesajı veriyor
- queue.ts'te "gemini-flash" yok
- TypeScript build hatasız
</success_criteria>

<output>
Tamamlanınca `.planning/phases/24-model-config-centralization/24-01-SUMMARY.md` oluştur:

# Phase 24 Plan 01: Backend Model Config Centralization Summary

**[Tek cümle — ne değişti]**

## Accomplishments
- [...]

## Files Created/Modified
- `functions/src/orchestrator/types.ts` — 4 yeni SystemSettings alanı
- `functions/src/orchestrator/seed/defaultData.ts` — yeni alan defaults
- `functions/src/controllers/orchestrator/shared.ts` — Firestore'dan imageModel
- `functions/src/schedulers/orchestratorScheduler.ts` — getOrchestratorConfig() kullanımı
- `functions/src/services/visualCriticService.ts` — hardcoded kaldırıldı
- `functions/src/services/posterLearningService.ts` — hardcoded kaldırıldı
- `functions/src/controllers/orchestrator/posterSmartController.ts` — değişken kullanımı
- `functions/src/controllers/orchestrator/enhancementController.ts` — fallback kaldırıldı
- `functions/src/services/queue.ts` — eski model adı güncellendi

## Decisions Made
[...]

## Issues Encountered
[...]

## Next Step
Ready for 24-02-PLAN.md (Admin Panel)
</output>
